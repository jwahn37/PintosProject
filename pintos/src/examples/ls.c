/* halt.c

   Simple program to test whether running a user program works.
 	
   Just invokes a system call that shuts down the OS. */

#include <syscall.h>

int
main (void)
{
//  halt ();
//	wait(exec("ls -l foo bar"));
//	char buf[5];
//	read(0, &buf, 5);
//	write(1, &buf, 5);
	//write(1, "abcde", 5);
//	halt();
//	printf("hello my name is halt from user process!\n");
	exit(5);
//		exec(4);
//	wait(4);
///		exec("hello.txt");
//		write (10,20,30);
//	exit (1); 
 /* not reached */
}
